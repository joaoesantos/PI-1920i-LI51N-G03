
const fs = require('fs')

fs.readFile('./fs-module.js', processFile)

let configFile = undefined;

function iterateDirectory() {

}

function getConfigFile() {
    configFile = JSON.parse(fs.readFile.)
    alert(configFile[0].api_key);
    alert(configFile[1].url);
    alert(configFile[2].searchByPopularity);
    alert(configFile[3].port);
}

function getServerConfig(){
    return { port : configFile.port }
}

function getApiConfig(){
    return {
        api_key : configFile.api_key,
        url : configFile.url,
        searchByPopularity : configFile.searchByPopularity
    }
}

module.exports = configFile;